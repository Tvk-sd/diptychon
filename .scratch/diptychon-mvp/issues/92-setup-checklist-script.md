# 92 — Setup-Checkliste als Skript (`scripts/setup.sh`)

> *This was generated by AI during a chat session (no pre-existing issue).*

Status: **ready-for-human** (nur noch der 2-Minuten-Test auf Tills Mac offen)
Category: dev-ergonomics

## Warum

Till vergisst nach dem Klonen (und nach jedem `project.yml`-Edit /
neuen `.swift`-File) regelmäßig das Setup-Ritual — verständlich, denn das
`.xcodeproj` ist generiert und gitignored, es gibt also nichts Sichtbares,
das daran erinnert. Bisher stand die Abfolge nur im README-Fließtext.

## Was geliefert wurde

- `scripts/setup.sh`: ein idempotenter Befehl — prüft macOS, installiert
  XcodeGen via Homebrew falls nötig, führt `xcodegen generate` aus und
  druckt die nächsten Schritte (Xcode öffnen, xcodebuild-Kommandos,
  Full-Disk-Access- und `DIPTYCHON_DIR`-Hinweise).
- README › „Install & run" zeigt jetzt zuerst `./scripts/setup.sh`,
  die Handschritte bleiben als Klammerzusatz erhalten.

## Outcome

Geliefert in Commit `3770c35` auf Branch `claude/setup-checklist-xg3vzo`.
Offen bei Till: einmal `./scripts/setup.sh` auf dem eigenen Mac laufen
lassen und bestätigen, dass Xcode-Build danach durchgeht (remote nicht
testbar — Linux-Container ohne macOS/Homebrew/Xcode).
