.build/
.build-dd/
build/
*.app/
.DS_Store
*.xcodeproj
.swiftpm/
DerivedData/

# Local presentation/scratch artifacts (not part of the app)
presentations/

# Wrangler local state (Cloudflare dev cache)
.wrangler/

# Stray build artifact in repo root (the real download lives in .scratch/landing-page/)
/Diptychon.zip
