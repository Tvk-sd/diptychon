# context/

Working & research docs for in-progress features — inputs that inform a plan but
aren't the spec or the code.

- `sidebar-research.md` — (drop here) research/perspective for the left sidebar feature.
- Per the repo convention, `PLAN.md` lives at the repo root; if a task needs
  scratch research or reference material, it goes here.

Not to be confused with the root `CONTEXT.md` (the single domain doc) or
`.scratch/diptychon-mvp/` (issues + PRD).

`archive/` — retired state docs, kept for history only (PROJECT-TRACKER through
2026-07-12, session retros). Outcomes now close in the issue files / ADRs;
open items live in root `PLAN.md`.
